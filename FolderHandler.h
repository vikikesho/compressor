#pragma once
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

void Combine(ifstream& input, string input_name, ofstream& output);
void Separate(ifstream& input);

